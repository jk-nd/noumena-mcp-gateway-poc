$schema: https://documentation.noumenadigital.com/schemas/migration-schema-v2.yml

changesets:
  - name: mcp-1.0
    changes:
      - migrate:
          sources:
            - ../npl-1.0
          rules: rules.yml
  
  # Added validateForExecution, reportCompletion, and request tracking
  - name: mcp-1.1-validation
    changes:
      - migrate:
          sources:
            - ../npl-1.0
          rules: rules.yml
  
  # Added pExecutor as separate party for proper separation of concerns
  - name: mcp-1.2-executor-party
    changes:
      - migrate:
          sources:
            - ../npl-1.0
          rules: rules.yml
  
  # Added pExecutor party assignment rules
  - name: mcp-1.3-executor-rules
    changes:
      - migrate:
          sources:
            - ../npl-1.0
          rules: rules.yml
