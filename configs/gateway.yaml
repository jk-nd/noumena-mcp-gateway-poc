# Gateway Configuration
# V2: Transparent proxy — no Vault access, no callback endpoint.
# Credentials are handled by a separate Credential Proxy (future).

server:
  port: 8080

oidc:
  issuer: ${OIDC_ISSUER:-http://keycloak:8080/realms/noumena}
  audience: noumena-mcp-gateway

npl:
  url: ${NPL_URL:-http://npl-engine:8080}
  timeout_ms: 30000

policy:
  mode: required  # required | optional | audit_only

logging:
  level: INFO
