# MCP Aggregator — Multi-backend routing gateway
#
# Routes MCP requests to multiple backend MCP servers:
#   POST   /mcp  — JSON-RPC routing by tool namespace
#   GET    /mcp  — SSE multiplexing from all backends
#   DELETE /mcp  — Session termination

FROM node:22-slim

WORKDIR /app

COPY mcp-aggregator/package.json ./
RUN npm install --production

COPY mcp-aggregator/server.js ./

EXPOSE 8000

CMD ["node", "server.js"]
